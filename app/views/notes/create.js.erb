$('.panel-group').prepend("<%= j render 'notes/note', note: @note %>");
$("button[data-target='#myNoteModal']").after("<%= j render 'notes/form', note: @note, stock: @note.stock %>");
$('#myNoteModal').modal('hide');